import { createUseSortRepositoriesList } from '../../shared/useSortReposTable';
import { COLUMNS_TO_SORT } from './constants';

export const useSortRepositoriesList = createUseSortRepositoriesList(COLUMNS_TO_SORT);
